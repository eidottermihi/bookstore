// Umgebung für Build

// Artifactory
binaryRepository {
  url = "http://localhost:8081/artifactory"
  username = "admin"
  password = "password"
  name = "libs-release-local"
}

environments {
  test {
    server {
      hostname = 'localhost'
      port = 11080
      context = 'bookstore'
      username = 'tomcat'
      password = 's3cret'
    }
  }

  uat {
    server {
      hostname = 'localhost'
      port = 12080
      context = 'bookstoreuat'
      username = 'tomcat'
      password = 's3cret'
    }
  }

  prod {
    server {
      hostname = 'localhost'
      port = 13080
      context = 'bookstoreprod'
      username = 'tomcat'
      password = 's3cret'
    }
  }
}